#include <stdio.h>
#include <string.h>

int main(){
    Initialize testcase variable, stepCount variable, and AdamInput variable;

    print instruction before for user to know what to input;

    for(int i is less than testcase){
        request for an input and save it in AdamsInput;
        check for the length of AdamsInput;


        for(int j is less than the length of AdamInput){
            loop through the AdamInput value and compare;
            if(AdamsInput == U){
                increase the number of Initialize stepCount variable above;
            }else{
                print total number of stepCount so far before and D;
                then set back the stepCount to zero;
                break the loop;
            }
        }
    }

    return 0;
}

